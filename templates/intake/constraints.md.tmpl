# Constraints

Hard stops and access constraints that shape what's actually available.

<!-- TODO: fill in next session. Cover: contraindications (medical: e.g., pregnancy, comorbidities; AE-history: drugs that cannot be re-tried), access constraints (insurance coverage, prior-auth landscape, geographic limits on specialists, cost), patient-side constraints (work schedule, caregiver responsibilities, side-effect tolerance). -->
