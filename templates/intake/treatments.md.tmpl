# Treatment-history interview

This file is the raw interview output. The normalized class-by-class catalog lives at `treatments/tried.md`.

<!-- TODO: fill in a dedicated treatment-history-interview session. For each treatment ever tried: name, dates, dose/intensity, duration, why started, why stopped (efficacy or AE), adequacy classification (see `meta/standards.md` for categories), source of memory (chart / patient recall / pharmacy record). -->
